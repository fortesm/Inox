
- empty-paren-declaration.inox rejects Main() declarations; use Main.
- empty-paren-call.inox rejects Report() calls; use Report.
- empty-paren-method-call.inox rejects Account.Print() calls; use Account.Print.
